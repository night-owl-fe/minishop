/* 按钮的默认样式 */
.btn-get {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  padding: 3.5rpx 12rpx;
  font-size: 24rpx;
  font-weight: 500;
  border-radius: 8rpx;
  color: #333;
  background-color: #fff;
  box-shadow: 1rpx 1rpx 2rpx 0 #999;
}

.btn-get:after {
  display: none;
}
