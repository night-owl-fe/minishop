/* 按钮的默认样式 */
.btn-get {
  position: absolute;
  top: 20px;
  right: 20px;
  padding: 3.5px 12px;
  font-size: 24px;
  font-weight: 500;
  border-radius: 8px;
  color: #333;
  background-color: #fff;
  box-shadow: 1px 1px 2px 0 #999;
}

.btn-get:after {
  display: none;
}
